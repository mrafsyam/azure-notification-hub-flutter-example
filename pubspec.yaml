name: azure_notification_hub_poc
description: A demo Flutter App to receive in-App notification from Azure Notification Hub

#publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # the original repository was deleted, had to fork one just in case
  azure_notificationhubs_flutter:
    git:
      url: git://github.com/mrafsyam/azure-notificationhubs-flutter.git
      ref: 94cedcf05693a4ff74241f1c885af607ae09a6f0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

